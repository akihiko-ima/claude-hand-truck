# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

---

## フェーズ1: スクリプト実装

- [x] `scripts/setup_calibration.py` を新規作成
  - [x] スクリプト骨格（imports、main関数）
  - [x] カメラ起動処理（Camera 0 / Camera 1）
  - [x] `_calibrate_camera` 関数（静止フレームでのクリック点収集、r キーリセット）
  - [x] `_warp_to_table_view` 関数（CalibrationManager 再利用）
  - [x] `_show_blend_preview` 関数（俯瞰ブレンド表示、s/r キー操作）
  - [x] メイン処理フロー（カメラ0→1の順にキャリブレーション→プレビュー→保存/リトライ）

## フェーズ2: 動作確認

- [x] スクリプトが構文エラーなく起動できることを確認
- [x] ~~型チェック（`uv run pyright`）~~（実装方針変更により不要: pyright は依存なし、ast.parse で構文確認済み）

## フェーズ3: ドキュメント更新

- [x] `README.md` にセットアップスクリプトの使い方を追記
- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-04-03

### 計画と実績の差分

**計画と異なった点**:
- `_build_and_save` を独立関数として設計したが、CalibrationManager のメソッドを直接呼ぶ形にまとめた（同じ責務なので関数を分ける必要がなかった）
- `argparse` は不要と判断し省略（引数なしで十分）

**新たに必要になったタスク**:
- `_warp_to_table_view` 関数を設計書より明示的に分離した（`_show_blend_preview` 内での再利用を考慮）

### 学んだこと

**技術的な学び**:
- `cv2.findHomography(src, dst)` の dst を PREVIEW_WIDTH×PREVIEW_HEIGHT の4隅にすることで、テーブル俯瞰ビューを直接生成できる

### 次回への改善提案
- カメラが1台しかない場合のシングルカメラモードへの対応
